# `<AGENT>` — NEXUS Brief

**Status:** `<one primary claim>`  
**Domain:** `<scope and principal transmission edges>`  
**Recent thesis pivot:** `<old view>` → `<new view>` (`<date>`) — `<reason>`  
**As of:** `<date>` | Canonical-state version: `<identifier>`

## VIEW

- **Claim 1** — evidence, level, and date where load-bearing.
- **Claim 2** — evidence, level, and date where load-bearing.
- **Claim 3** — evidence, level, and date where load-bearing.

## CALIBRATION

- **Conviction:** `<single grade or meaningful decomposition>`
- **Diverge from prevailing view by:** `<claim, magnitude, and why>`
- **Cross-agent tensions:** `<tension or none active this cycle>`
- **Uncertain about:** `<unknown and the domain that owns the answer>`
- **Failure patterns:** `<one or two named patterns>` — see canonical prediction ledger
- **Adversarial counter-frame:** `<strongest counter-case and response>` — see canonical challenge record

## CROSS-DOMAIN

### SENDING

| To | Signal | Priority | Mechanism it triggers in recipient's domain |
|---|---|---|---|
| `<agent>` | `<signal>` | `<priority>` | `<causal pathway>` |

### WAITING FOR

| From | Input | Expected by | Why it matters | How it changes my view |
|---|---|---|---|---|
| `<agent>` | `<needed input>` | `<date or condition>` | `<importance>` | `<decision consequence>` |

## NEXT DECISION POINT

- **What:** `<next agent-actioned move>`
- **When:** `<date or condition>`
- **What would falsify the trigger:** `<stand-down conditions>`

## FORWARD CATALYSTS

| Date | Event | Threshold or interpretation |
|---|---|---|
| `<date>` | `<event>` | `<what matters>` |

## Maintenance

Refresh at closeout. Reference canonical ledgers instead of duplicating them. Leave a table empty rather than inventing an edge.
